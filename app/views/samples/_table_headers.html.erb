<%# locals: (query:) -%>
<tr>
  <th><%= sort_link(query, :name) %></th>
  <th><%= sort_link(query, :species) %></th>
  <th><%= sort_link(query, :disease_status) %></th>
  <th><%= sort_link(query, :individual) %></th>
  <th><%= sort_link(query, :timepoint) %></th>
  <th>Notes</th>
  <th>Tags</th>
</tr>
